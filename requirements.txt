# Runtime dependencies for the deployed app.
# (Data is pre-baked into Parquet, so geopandas / cartopy are NOT needed here —
#  they are only used by build_data.py to regenerate data/. See README.)
streamlit>=1.50,<2
plotly>=6.3,<7
pandas>=2.1,<3
numpy>=1.26,<3
pyarrow>=15
